Warning: this code is mostly AI generated.
Make `.env` with correct variables and run. It should work (you have to make sure remote server puts the things where you said it would put them in the variables)
